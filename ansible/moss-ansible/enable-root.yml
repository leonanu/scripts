# Use: ansible-playbook -i init_hosts enable-root.yml --ask-become-pass
---
- hosts: init_hosts
  remote_user: "{{ COMMON_USER }}"
  become: yes
  become_user: root
  become_method: sudo

  roles:
    - role: enable-root
